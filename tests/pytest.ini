[pytest]
asyncio_mode = auto
timeout = 60
markers =
    integration: marks tests as integration tests (require running services)
    slow: marks tests as slow (full pipeline, >10s)
    unit: marks tests as unit tests (no external services)
testpaths = integration
log_cli = true
log_cli_level = INFO
addopts = -v --tb=short
